# G-Dice Intelligence Federation — Directory Contract v0.1

STATUS = SCAFFOLD_ONLY
AUTHORITY_CREATED = FALSE
INTELLIGENCE_AUTHORITY_CREATED = FALSE
LAW_ENFORCEMENT_AUTHORITY_CREATED = FALSE
OPERATIONAL_TASKING = FALSE
CLASSIFIED_ACCESS = FALSE

Purpose: hold a public, educational hypothesis/replay model that separates question generation from evidence evaluation.

Topology:

- `agency_dice/` — public mission-domain hypothesis schemas; no claim about any agency's actual internal analytic methods.
- `reconciliation/` — cross-source / cross-agency disagreement handling.
- `receipts/` — replayable public-source evidence receipts.
- `tests/` — fail-closed invariants.

Core membrane:

RANDOMNESS MAY SELECT A QUESTION.
RANDOMNESS MUST NEVER DECIDE WHETHER A CLAIM IS TRUE.

PUBLIC MODEL != GOVERNMENT SYSTEM
AGENCY-LABELED DIE != AGENCY ENDORSEMENT
PUBLIC MISSION MAPPING != INTERNAL METHOD
HYPOTHESIS != INTELLIGENCE FINDING
INTELLIGENCE FINDING != CRIMINAL FINDING
SUSPICION != TARGET
NATIONALITY != THREAT

The official U.S. Intelligence Community organization count must be source-bound independently from this model. Logical dice count may differ from organization count.
