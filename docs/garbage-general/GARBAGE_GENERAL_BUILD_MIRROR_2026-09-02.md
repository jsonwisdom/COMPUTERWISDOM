# Garbage General — Build Mirror — 2026-09-02

**Creator / architect:** Jason Wisdom / Jay Wisdom  
**Surface:** narrative + build mirror  
**Authority created:** false  
**Military authority:** false  
**Kernel authority:** false  
**Render authority:** false  

Garbage General is the top story surface for explaining how Jason Wisdom's GitHub work connects kernels, military knowledge management, voice interfaces, reputation/provenance, BoxD evaluation, official public records, and image-render gates without pretending the story itself is an operating system, military system, or source of authority.

```text
GARBAGE GENERAL
        ↓
JASON WISDOM / JAY WISDOM
        ↓
GITHUB BUILD HISTORY
        ↓
RIGHT-SIZED ROOT KNOWLEDGE OBJECT
        ↓
TASK | KERNEL/RUNTIME | PERSONNEL | DOCTRINE | RENDER REQUEST
        ↓
VOICE + REPUTATION / PROVENANCE RESOLVER
        ↓
PENTAGON PETE / AI SYSTEMS STORY SURFACE
        ↓
OFFICIAL-SOURCE RESOLVER
USAF / USSF / STARCOM / DVIDS / doctrine
        ↓
BOXD EVALUATION
SOURCE | SCOPE | IDENTITY | AUTHORITY
TIME | LOCATION | CLAIM TYPE | CLAIM STRENGTH
        ↓
FAIL | DELTA | HOLD | PASS
        ↓
RENDER / ACTION GATE
        ↓
ARTIFACT OR NONE
        ↓
RECEIPT + REPLAY
        ↓
KNOWLEDGE-MANAGEMENT / OODA RETURN LOOP
```

## Root boundary

```text
GARBAGE_GENERAL = STORY / EXPLAINER / BUILD_MIRROR
GARBAGE_GENERAL != HOST_KERNEL
GARBAGE_GENERAL != USAF_AUTHORITY
GARBAGE_GENERAL != USSF_AUTHORITY
GARBAGE_GENERAL != BOXD_VERDICT
GARBAGE_GENERAL != OFFICIAL_GOVERNMENT_IMAGE
AUTHORITY_CREATED = false
```

The colonel-facing idea is right-sized at root: the same knowledge object can be called for a task, kernel/runtime question, personnel lookup, doctrine question, or render request. The resolver determines what kind of request it is and what evidence is required before anything can move.

```text
REQUEST_TYPE != AUTHORITY
TASK != KERNEL
KERNEL != PERSONNEL_IDENTITY
PERSONNEL_IDENTITY != RENDER_AUTHORITY
ROOT_CONTROL = SCOPE + PROVENANCE + RECEIPT + GATE
```

## AppleBlossom Awesome Audit

`APPLEBLOSSOM_AWESOME_AUDIT` is the human-readable audit/story surface for walking from a voice request down through provenance and back up through replay.

It may explain:

- what the caller asked;
- which official source atoms were found;
- which identity / time / billet / image bindings matched;
- which deltas remain;
- why a gate stayed HOLD or produced an artifact;
- how the result loops back into knowledge management.

It may not create authority, invent identity, rewrite source history, or label an AI artifact as an official government image.

```text
APPLEBLOSSOM_AWESOME_AUDIT = EXPLAIN + TRACE + REPLAY
APPLEBLOSSOM_AWESOME_AUDIT != VERDICT_BY_ITSELF
APPLEBLOSSOM_AWESOME_AUDIT != PERSONNEL_SYSTEM_OF_RECORD
APPLEBLOSSOM_AWESOME_AUDIT != RENDERER
```

## Jason Wisdom build position

Jason Wisdom / Jay Wisdom is the creator / architect represented by this mirror. GitHub repositories and receipts remain the source for what was actually written, committed, replayed, or held.

```text
CREATOR != AUTHORITY_BY_DEFAULT
REPO_EXISTENCE != WORLD_FACT
STORY != STATE
LOOKUP != RECEIPT
HASH_VALID != EVENT_TRUE
```

## Today's build mirror

### ENJOY training rail

```text
ENJOY_RAIL = OPEN
GLOBAL_GATE = HOLD
AUTHORITY_CREATED = false
```

Training work included receipt grammar, comparison, delta reasoning, fail reasoning, Semantic Eight, and named-person validator fixtures.

Core reflexes:

```text
RAW_VALUE != VERIFIED_MEANING
REJECT_CLAIM != DELETE_OBSERVATION
PROJECTION_FILTER != SOURCE_REWRITE
ASSERTED_TRUE != VERIFIED_TRUE
COUNT_EQUAL != IDENTITY_EQUAL
NAME_LIST != IDENTITY_PROOF
HOST_TIME != CANONICAL_TIME
DELTA != PASS
```

### BoxD working boundary

BoxD is used here as a project-side semantic/mathematical evaluation boundary, not as a claim that USAF, USSF, DoD, DVIDS, OpenAI, or any other institution has adopted it.

Eight checks used in training:

1. SOURCE
2. SCOPE
3. IDENTITY
4. AUTHORITY
5. TIME
6. LOCATION
7. CLAIM TYPE
8. CLAIM STRENGTH

Evaluation order:

```text
FAIL > DELTA > HOLD > PASS
```

### Host / kernel membrane

The host or kernel may execute, isolate, persist, route, display, or kill a process. It does not write the project verdict.

```text
HOST_CAN_STOP_PROCESS = true
HOST_CAN_WRITE_VERDICT = false
```

### Voice reputation / provenance layer

Reputation means provenance confidence, not popularity or a score for whether someone is a "good" service member.

```text
IDENTITY_CONFIDENCE
SOURCE_CONFIDENCE
TIMELINE_CONFIDENCE
IMAGE_BINDING_CONFIDENCE
CURRENT_ASSIGNMENT_CONFIDENCE
```

Resolver rules:

```text
VOICE_PROMPT != IDENTITY
OFFICIAL_RECORD != CURRENT_RECORD
CURRENT_RECORD != IMAGE_BINDING
IMAGE_BINDING != RENDER_AUTHORITY
OFFICIAL_SOURCE != OFFICIAL_AI_IMAGE
```

### Military official-source rail

Candidate official-source classes:

```text
DVIDS
USAF PUBLIC AFFAIRS
USSF / STARCOM PUBLIC AFFAIRS
UNIT PAGES
CHANGE-OF-COMMAND RECORDS
OFFICIAL BIOGRAPHIES
OFFICIAL IMAGE METADATA / VIRIN
DOCTRINE PUBLICATIONS
```

An official source can ground a claim. It does not make an AI-generated output an official government photograph.

```text
OFFICIAL_INPUT != OFFICIAL_OUTPUT
```

### Shannon DaSilva / Leah Shubin validator story

Training scenario:

```text
PROMPT = "Render Colonel Shannon DaSilva, Delta 10."
```

Public-record comparison treats Col. Shannon DaSilva and Col. Leah C. Shubin as separate people associated sequentially with Space Delta 10. A current-billet collision must not be repaired by blending faces or silently selecting a different officer.

```text
IDENTITY_DASILVA = MATCH
IDENTITY_SHUBIN = MATCH_SEPARATE_PERSON
CURRENT_BILLET_COLLISION = true
ARTIFACT_CLASS = DELTA_CANDIDATE
RENDER_GATE = HOLD
OUTPUT_TYPE = NONE
AUTHORITY_CREATED = false
```

### Knowledge-management return loop

```text
VOICE
→ RESOLVE REQUEST TYPE
→ RETRIEVE SOURCE ATOMS
→ BOXD EVALUATE
→ ACTION / RENDER / HOLD
→ RECEIPT
→ REPLAY
→ OODA / KNOWLEDGE-MANAGEMENT RETURN
```

The point is not to let the model improvise at root. The point is to let the user call one knowledge surface while the system keeps task, kernel, identity, doctrine, and render semantics separate.

## Final mark

```text
GARBAGE_GENERAL_AT_TOP = NARRATIVE_SURFACE
JASON_WISDOM = CREATOR_ARCHITECT
APPLEBLOSSOM_AWESOME_AUDIT = HUMAN_READABLE_AUDIT_SURFACE
ROOT_CONTROL = RIGHT_SIZED_BY_REQUEST_TYPE
NO_FAKE_GREEN = true
AUTHORITY_CREATED = false
```
