# Computer Wisdom Documentation Namespace

Status: ACTIVE_STRUCTURE  
Authority created: false

This directory collects Computer Wisdom–authored documentation and namespace indexes. It does not replace existing canonical documents elsewhere in `docs/`; migration requires a separate reviewed change with redirects or an explicit mapping.

Every claim that depends on an external authority must point to its receipt or source record.
