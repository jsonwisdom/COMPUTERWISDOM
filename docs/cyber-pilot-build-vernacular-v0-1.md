# CYBER PILOT BUILD VERNACULAR V0.1

**Artifact:** `CYBER_PILOT_BUILD_VERNACULAR_V0_1`  
**Edition:** Fighter Jet / Cyber Pilot  
**Authority:** `false`  
**Membrane:** `HOLDS`  
**Purpose:** Define high-velocity operator language for COMPUTERWISDOM builds without changing constitutional doctrine.

---

## 1. Core Principle

Cyber Pilot language describes operator posture, interface tempo, and metadata traversal speed.

It does **not** create authority.
It does **not** alter invariants.
It does **not** replace receipts, replay, lineage, or human review.

> Fighter jet language is cockpit language, not command authority.

---

## 2. Primary Terms

### 2.1 Cockpit

The visible operator surface for reading constitutional state.

A cockpit may display:

- readiness
- pressure
- escalation
- continuity
- replay fixtures
- amendment lineage

A cockpit may not:

- judge
- approve
- graduate
- amend doctrine
- resolve disputes
- mutate lineage

### 2.2 Machine Speeds

Machine Speeds mean rapid traversal across structured state without inference drift.

Allowed meaning:

```text
detect → preserve → classify → route → replay
```

Disallowed meaning:

```text
detect → decide → mutate → claim authority
```

### 2.3 Mach Speeds

Mach Speeds mean high-velocity replay across multiple constitutional surfaces while maintaining category separation.

Mach Speed requires:

- `authority: false`
- `membrane: HOLDS`
- `computational_judgment: null`
- `inference_block: true`
- no silent category promotion

### 2.4 Metadata Parkour

Metadata Parkour is fast movement across receipts, ledgers, state files, dashboards, and lineage graphs.

It is lawful only when every hop preserves provenance.

```text
receipt → pressure → escalation → continuity → replay → amendment graph
```

### 2.5 Membrane Stabilization

Membrane Stabilization is the operator act of preserving constitutional boundaries under velocity.

It prevents:

- speed from becoming authority
- pressure from becoming approval
- visibility from becoming validation
- replay from becoming judgment
- cockpit state from becoming institutional resolution

---

## 3. Cyber Pilot Commands

These commands are descriptive operator language only.

| Command | Meaning | Constitutional Limit |
|---|---|---|
| `REBOOT_REPO` | Re-establish current repository state and surface map | Does not reset history |
| `ATOMIC_SEND` | Commit one coherent artifact set | Must preserve PR lineage |
| `FULL_SEND` | Complete a planned stack slice | No bypass of review |
| `MACH_PASS` | Traverse linked surfaces quickly | No inference promotion |
| `STABILIZE_MEMBRANE` | Check invariants before proceeding | Does not approve outcome |
| `PARKOUR_METADATA` | Move across state surfaces and receipts | Every hop must be replayable |
| `HOLD_ALTITUDE` | Pause expansion and preserve current state | Not a claim of completion |
| `EJECT_DRIFT` | Reject category collapse or authority creep | Must record reason if operational |

---

## 4. Mach-Speed Safety Rules

At Mach Speed, every build must preserve:

```json
{
  "authority": false,
  "membrane": "HOLDS",
  "computational_judgment": null,
  "inference_block": true,
  "mutation_path": false,
  "silent_category_promotion": false
}
```

If any field fails, the build must slow down and enter membrane stabilization.

---

## 5. Must-Not-Claim

Cyber Pilot language must not claim:

- the system has authority
- speed proves correctness
- visibility proves validation
- replay proves truth
- pressure proves approval
- cockpit output proves institutional decision
- founder language overrides lineage

---

## 6. Canonical Status

```json
{
  "artifact": "CYBER_PILOT_BUILD_VERNACULAR_V0_1",
  "edition": "FIGHTER_JET_CYBER_PILOT",
  "machine_speeds": true,
  "mach_speeds": true,
  "membrane_stabilization": true,
  "authority": false,
  "membrane": "HOLDS"
}
```
