# COMPUTERWISDOM Executables

Canonical discovery surface for reusable runnable entrypoints.

Project-local scripts are allowed when they are genuinely project-specific. A reusable command should be promoted here (or exposed here through a stable wrapper) instead of remaining buried under a mission, receipt, experiment, or product subtree.

Libraries are not required to live here.

`EXECUTABLE != AUTHORIZATION`
