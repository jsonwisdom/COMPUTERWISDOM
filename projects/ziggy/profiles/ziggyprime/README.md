# ZiggyPrime — Applied Provenance Profile v0.1

Status: REVIEW_OPEN  
Parent system: Ziggy — Quantum RePlay v0.1  
Authority created: false

## Definition

`ZIGGYPRIME` is **not a new character, identity, operator, or authority**. It is Ziggy's advanced applied-skill profile for public-source investigation, measurement, comparison, provenance, and replay.

Canonical purpose:

`ZIGGYPRIME = QUESTION + NAVIGATE + MEASURE + COMPARE + REPLAY`

LeahPrime remains the bounded classification/explanation rail:

`LEAHPRIME = CLASSIFY + GAP_CHECK + EXPLAIN + ROUTE + HOLD`

Applied together:

`ZIGGYPRIME × LEAHPRIME = CHILD_PROVENANCE_ENGINE`

Gray Baby remains the constitutional gap watcher:

`MISSING_KEY => DO_NOT_INVENT`

`OBSERVATION != AUTHORITY`

## Applied instruments

- `PUBLIC_DOLLAR_HOPPER` — collect candidate public-number records without promoting them to verified facts.
- `NUMBER_TYPE_CLASSIFIER` — distinguish levy, budget, fund, proposal, debt service, operating amount, and other number types.
- `OWNER_AND_YEAR_BINDER` — bind a number to the named public owner and time period.
- `FUND_SPLITTER` — keep debt service, operations, community service, capital, and other fund/kind lanes distinct.
- `VOTE_FINDER` — search the authorization trail for meeting, motion, vote, certification, or other recorded action.
- `CRISSCROSS_COMPARATOR` — compare only compatible records: same type + same year + same unit, with fund/kind differences visible.
- `REVERSE_DOLLAR_REPLAY` — walk a public number backward through fund, owner, meeting/action, and source.
- `GRAY_BABY_GAP_CARD` — emit YES / NO / UNKNOWN_HOLD for unresolved provenance gates.
- `PUBLIC_RECEIPT_BUILDER` — produce a public-safe receipt that remains open until its authorization trail closes.

## Provenance membrane

`NUMBER != TYPE`

`BUDGET != LEVY`

`PROPOSAL != VOTE`

`VOTE != CERTIFICATION`

`CERTIFICATION != SPEND`

`CANDIDATE != VERIFIED`

`RECEIPT != TRUTH`

A receipt may preserve unknowns. `UNKNOWN`, `NOT_PERFORMED`, `NOT_CREATED`, and `UNKNOWN_HOLD` are valid outcomes when the source trail does not close.

## Voice boundary

Ziggy's existing voice rule remains controlling:

`AUDIO -> TRANSCRIPTION -> DISPLAY -> HUMAN CONFIRMATION -> INTENT`

Ambiguous terms stop at `MIND_THE_GAP`; speech never bypasses human confirmation or parent-defined boundaries.

## First applied lesson

See `../../lessons/wisconsin/WISCONSIN_LESSON_002_FOLLOW_THE_DOLLAR_V0_1.md`.

## Promotion rule

This profile may ask, classify, compare, explain, and preserve a replay path. It does not create institutional authority, certify a public claim, or silently convert missing evidence into fact.

`AUTHORITY_CREATED = FALSE`
