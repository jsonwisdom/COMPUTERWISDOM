# JSONWisdom Base Sepolia deployment environment (NON-SECRET ONLY)
# Copy to .env locally if desired. .env is ignored by git.

BASE_SEPOLIA_RPC_URL=https://sepolia.base.org
FOUNDRY_ACCOUNT=
ETHERSCAN_API_KEY=

# Private keys and seed phrases are intentionally not represented here.
# Preferred testnet signer: encrypted Foundry keystore via `cast wallet import <name> --interactive`.
