# COMPUTERWISDOM Proofs

Canonical discovery surface for reproducible proof packets, verification outputs, and proof indexes.

A proof object must identify what was tested, the exact inputs or hashes, the verifier/instrument version, the observed result, and enough provenance for replay.

A filename, narrative, receipt, or hash by itself is not proof of the underlying claim.
